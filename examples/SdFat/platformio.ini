[platformio]
src_dir=SdFat

; [env:due]
; board=due
; platform = atmelsam
; framework = arduino
; upload_port=/dev/ttyACM0
; build_flags =
;   -DMENU_FMT_WRAPS
;   -DMENU_ASYNC

[env:uno]
platform=atmelavr
board=pro8MHzatmega328
framework=arduino
upload_protocol = usbasp
upload_flags = -Pusb
lib_ignore=WebSockets
