[env:adafruit_crickit_m0]
src_dir=Button_Navigation
board=adafruit_crickit_m0
platform = atmelsam
framework = arduino
; build_flags =
;   -DMENU_DEBUG
;   -Wno-strict-aliasing -Wno-sign-compare -Wno-write-strings

; [env:nanoatmega328]
; src_dir=Button_Navigation
; platform = atmelavr
; board = nanoatmega328
; framework = arduino
; upload_port=/dev/ttyUSB*
; upload_flags=-V
; upload_speed=57600
; build_flags = -DMENU_DEBUG
; src_build_flags = !echo "-Wno-write-strings -Wno-reorder -fno-strict-aliasing -DLOC="$PLATFORMIO_LOC

; [env:teensy31]
; src_dir=Button_Navigation
; platform = teensy
; board = teensy31
; framework = arduino
;; build_flags = -lstdc++

; [env:esp12e]
; src_dir=Button_Navigation
; platform = espressif8266
; board = esp12e
; framework = arduino
; upload_speed=921600
; build_flags = -lstdc++ -DDEBUG

; [env:wemos]
; src_dir=Button_Navigation
; platform = espressif8266
; board = d1_mini_pro
; framework = arduino
; ; upload_speed=1500000
; upload_speed=921600

; ── AM5 native-selftest port (src/main.cpp) — added alongside the original
; vendored envs above (each pinned to src_dir=Button_Navigation so they
; keep building the real vendored .ino unchanged); this env has no
; src_dir override, so it builds from the default src/ instead.
[env:native]
platform = native
build_flags =
	-g
	-std=c++17
	-Wno-comment
	-fno-pretty-templates
debug_build_flags = -O0 -ggdb3 -g3 -no-pie
	-Wno-comment
	-fno-pretty-templates
lib_deps =
	HAPI      = https://github.com/InternetOfPins/HAPI.git
	OneBit    = https://github.com/InternetOfPins/OneBit.git
	OnePin    = https://github.com/InternetOfPins/OnePin.git
	OneBus    = https://github.com/InternetOfPins/OneBus.git
	OneChip   = https://github.com/InternetOfPins/OneChip.git
	OneData   = https://github.com/InternetOfPins/OneData.git
	OneOutput = https://github.com/InternetOfPins/OneOutput.git
	OneItem   = https://github.com/InternetOfPins/OneItem.git
	OneParse  = https://github.com/InternetOfPins/OneParse.git
	OneMenu   = https://github.com/InternetOfPins/OneMenu.git
